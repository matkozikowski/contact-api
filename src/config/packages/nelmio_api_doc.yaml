nelmio_api_doc:
    documentation:
        info:
            title: Contact API
            description: Application for sending and reading message.
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
